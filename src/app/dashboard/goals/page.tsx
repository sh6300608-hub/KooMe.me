import { db } from "@/lib/db";import { requireOwner } from "@/lib/owner";export default async function GoalsPage(){const u=await requireOwner();const rows=await db.futureGoal.findMany({where:{userId:u.id},orderBy:[{targetDate:"asc"},{progress:"desc"}]});return <div className="mx-auto max-w-6xl"><h1 className="text-3xl font-semibold">Future Goals</h1><p className="mt-2 text-slate-400">Private by default. Publish only what belongs on your portfolio.</p><div className="mt-8 grid gap-4 md:grid-cols-2">{rows.length?rows.map(x=><article key={x.id} className="rounded-2xl border border-slate-800 bg-slate-900/50 p-6"><div className="flex justify-between gap-4"><h2 className="font-medium">{x.title}</h2><span className="text-xs text-slate-500">{x.category}</span></div><div className="mt-5 h-2 rounded bg-slate-800"><div className="h-full rounded bg-blue-500" style={{width:`${Math.max(0,Math.min(100,x.progress))}%`}}/></div><p className="mt-2 text-right text-xs text-slate-500">{x.progress}%</p>{x.notes&&<p className="mt-4 text-sm text-slate-400">{x.notes}</p>}</article>):<p className="col-span-full rounded-2xl border border-dashed border-slate-700 p-10 text-center text-slate-500">No goals yet.</p>}</div></div>}
