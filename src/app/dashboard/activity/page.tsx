import { db } from "@/lib/db";import { requireOwner } from "@/lib/owner";export default async function ActivityPage(){const u=await requireOwner();const rows=await db.activityLog.findMany({where:{userId:u.id},orderBy:{createdAt:"desc"},take:100});return <div className="mx-auto max-w-6xl"><h1 className="text-3xl font-semibold">Activity</h1><div className="mt-8 rounded-2xl border border-slate-800 bg-slate-900/50 p-5">{rows.map(x=><div key={x.id} className="flex flex-wrap justify-between gap-3 border-b border-slate-800 py-4 text-sm"><span><b>{x.action}</b>{x.entity&&<span className="ml-2 text-slate-500">{x.entity}</span>}</span><time className="text-xs text-slate-600">{x.createdAt.toLocaleString()}</time></div>)}{!rows.length&&<p className="p-8 text-center text-slate-500">No activity recorded yet.</p>}</div></div>}
