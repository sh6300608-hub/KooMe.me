import { db } from "@/lib/db";
import { requireOwner } from "@/lib/owner";
export default async function SkillsPage(){const u=await requireOwner();const skills=await db.skill.findMany({where:{userId:u.id},include:{category:true},orderBy:[{featured:"desc"},{sortOrder:"asc"},{name:"asc"}]});return <div className="mx-auto max-w-6xl"><h1 className="text-3xl font-semibold">Skills</h1><p className="mt-2 text-slate-400">Manage capabilities and their public visibility.</p><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{skills.length?skills.map(s=><article key={s.id} className="rounded-2xl border border-slate-800 bg-slate-900/50 p-5"><div className="flex justify-between"><h2 className="font-medium">{s.name}</h2><span className="text-xs text-slate-500">{s.proficiency}%</span></div><p className="mt-2 text-xs text-slate-500">{s.category.name}{s.featured?" · Featured":""}</p><div className="mt-4 h-1.5 rounded bg-slate-800"><div className="h-full rounded bg-blue-500" style={{width:`${Math.max(0,Math.min(100,s.proficiency))}%`}}/></div></article>):<div className="col-span-full rounded-2xl border border-dashed border-slate-700 p-10 text-center text-slate-500">No skills yet. Seed only real skills when you are ready.</div>}</div></div>}
