import { db } from "@/lib/db";import { requireOwner } from "@/lib/owner";export default async function NotificationsPage(){const u=await requireOwner();const rows=await db.notification.findMany({where:{userId:u.id},orderBy:{createdAt:"desc"},take:100});return <div className="mx-auto max-w-6xl"><h1 className="text-3xl font-semibold">Notifications</h1><div className="mt-8 space-y-3">{rows.map(x=><article key={x.id} className="rounded-2xl border border-slate-800 bg-slate-900/50 p-5"><div className="flex justify-between gap-4"><h2 className="font-medium">{x.title}</h2><time className="text-xs text-slate-600">{x.createdAt.toLocaleString()}</time></div>{x.body&&<p className="mt-2 text-sm text-slate-400">{x.body}</p>}</article>)}{!rows.length&&<p className="rounded-2xl border border-dashed border-slate-700 p-10 text-center text-slate-500">No notifications yet.</p>}</div></div>}
